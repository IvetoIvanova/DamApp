spring:
  datasource:
    url: jdbc:hsqldb:mem:test_db
    driver-class-name: org.hsqldb.jdbc.JDBCDriver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true
#  sql:
#    init:
#      mode: never
  security:
    user:
      name: test-user
      password: test-password

logging:
  level:
    org:
      hibernate:
        type:
          descriptor: TRACE
        SQL: DEBUG
      nodeValue: WARN
    blog: WARN

#  mvc:
#    hiddenmethod:
#      filter:
#        enabled: 'true'

security:
  remembered:
    key: test-remember-me-key

ads:
  api:
    baseUrl: test-ads-base-url

geonames:
  username: test-g-username

cloudinary:
  cloud_name: test-cloud-name
  api_key: test-api-key
  api_secret: test-api-secret

admin:
  password: test-admin-password
  email: test-admin-email

#server:
#  port: ${server_port}